# https://github.com/SublimeLinter/SublimeLinter-jshint/issues/10

# EITHER THIS
[[ -s ~/.nvm/nvm.sh ]] && . ~/.nvm/nvm.sh

# OR THIS - But not both
# export NVM_DIR="/Users/chris/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm
